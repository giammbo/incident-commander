apiVersion: v1
kind: Service
metadata:
  name: {{ include "ic.fullname" . }}
  labels: {{- include "ic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "ic.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
